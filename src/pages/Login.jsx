import { useState } from 'react';
import { LockKeyhole, User, ArrowRight } from 'lucide-react';
export default function Login({onLogin,configurationPending}){const[form,setForm]=useState({email:'',password:''});const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setError('');setLoading(true);try{await onLogin(form.email,form.password)}catch(err){setError(err.message)}finally{setLoading(false)}}return <main className="login-page"><div className="login-visual"><div className="login-brand"><span className="brand-mark big">T</span><strong>TATAME</strong></div><div><span className="eyebrow">JIU-JITSU FIGHT MANAGER</span><h1>Confrontos certos.<br/><em>Eventos memoráveis.</em></h1><p>Organize atletas, encontre adversários compatíveis e gerencie cada luta em um só lugar.</p></div><small>GESTÃO INTELIGENTE • DECISÃO DO ORGANIZADOR</small></div><form className="login-card" onSubmit={submit}><span className="eyebrow">ACESSO RESTRITO</span><h2>Bem-vindo de volta</h2><p>Entre com o administrador criado no Supabase.</p><label>Email<div className="input-icon"><User size={18}/><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="admin@exemplo.com" autoFocus/></div></label><label>Senha<div className="input-icon"><LockKeyhole size={18}/><input required type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Digite sua senha"/></div></label>{configurationPending?<div className="form-error">Configuração do Supabase pendente. Crie o arquivo .env.</div>:null}{error?<div className="form-error">{error}</div>:null}<button disabled={loading||configurationPending} className="btn primary login-btn">{loading?'Entrando...':'Entrar'} <ArrowRight size={19}/></button></form></main>}
