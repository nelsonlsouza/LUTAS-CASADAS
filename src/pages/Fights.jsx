import { useState } from 'react';
import FightCard from '../components/FightCard';
export default function Fights({fights,athletes,onResult,onEdit,onDelete}){const [filter,setFilter]=useState('Todas');const visible=fights.filter(f=>filter==='Todas'||f.status===filter||f.modalidade===filter);return <><div className="tabs">{['Todas','Agendada','Finalizada','Gi','No-Gi'].map(v=><button className={filter===v?'active':''} onClick={()=>setFilter(v)} key={v}>{v}{v==='Todas'&&` (${fights.length})`}</button>)}</div><div className="fights-grid">{visible.map(f=><FightCard key={f.id} fight={f} a1={athletes.find(a=>a.id===f.atleta1Id)} a2={athletes.find(a=>a.id===f.atleta2Id)} onResult={()=>onResult(f)} onEdit={()=>onEdit(f)} onDelete={()=>onDelete(f)}/>)}</div>{!visible.length&&<div className="empty large">Nenhuma luta nesta categoria.</div>}</>}
